name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm ci

      - name: Clean previous build
        run: rm -rf dist pages-dist

      - name: Build
        run: npm run build

      - name: Prepare Pages artifact
        run: npm run pages:prepare

      - name: Verify deployment output
        run: |
          du -sh pages-dist
          du -sh pages-dist/*
          find pages-dist -type f | wc -l
          find pages-dist -type f -printf '%s %p\n' | sort -nr | sed -n '1,30p'
          npm run pages:verify

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./pages-dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
